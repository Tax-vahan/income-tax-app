services:
  tds-pan-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: tds-pan-api:latest
    container_name: tds-pan-api

    # ── Ports ──────────────────────────────────────────────────────────
    ports:
      - "8001:8001"

    # ── Environment ────────────────────────────────────────────────────
    # To inject secrets without hardcoding, create a .env file:
    #   echo "TDS_TAN=XXXX12345X"    >> .env
    #   echo "TDS_PASSWORD=yourpw"   >> .env
    # Then uncomment the env_file line below.
    env_file: .env
    environment:
      PYTHONUNBUFFERED: "1"
      DATA_DIR: "/app/data"
      DOWNLOADS_DIR: "/app/downloads"
      FILED_FORMS_DIR: "/app/filed_forms"
      CHROME_BIN: "/usr/bin/chromium"
      CHROMEDRIVER_BIN: "/usr/bin/chromedriver"

    # ── Shared memory — headless Chromium requires ≥512 MB ────────────
    shm_size: "2gb"

    # ── Persistent volumes ────────────────────────────────────────────
    # Mount the whole /app/data directory so Docker never mistakes
    # a missing file for a directory.  All runtime outputs live here.
    volumes:
      - ./data:/app/data
      - ./downloads:/app/downloads
      - ./filed_forms:/app/filed_forms

    # ── Restart policy ────────────────────────────────────────────────
    restart: unless-stopped

    # ── Log rotation ──────────────────────────────────────────────────
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "3"
